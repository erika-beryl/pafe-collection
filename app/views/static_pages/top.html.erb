<div class="top-wrapper">
  <div class="container pt-3">
    <!-- サイトの見出し -->
    <div class="frame-box">
      <h1>パフェコレへようこそ！</h1>
      <p>パフェコレは、パフェのレビューサイトです。<br>
        期間限定や地域限定のパフェ情報を登録して、<br>
        自分好みのパフェを見逃さないようにしましょう。</p>
    </div>
    <!-- 最新のパフェ情報三件 -->
    <div class="mt-5">
      <h4>パフェの新着情報</h4>
    </div>
    <div class="row mb-5">
      <div class="col-12">
        <div class="row">
          <%if @parfaits.present? %>
            <% @parfaits.each do |parfait| %>
              <div class="col-sm-12 col-lg-4 mb-3">
                 <div id="parfait-id-<%= parfait.id %>">
                  <div class="card">
                    <% if parfait.parfait_image.present? %>
                      <div class="parfait-image-wrapper">
                        <%= image_tag parfait.parfait_image, class: "img-fluid", height:"200" %>
                      </div>
                    <% else %>
                      <%= image_tag 'noimage.png', class: "card-img-top", width: "300", height:"200" %>
                    <% end %>
                    <div class="card-body">
                      <div class="d-flex">
                        <h4 class="card-title">
                          <%= link_to parfait.name, parfait_path(parfait) %>
                        </h4>
                        <% if user_signed_in? %>
                          <div class='ms-auto'>
                            <!-- rspecテストのためにid必要です -->
                            <%= link_to edit_parfait_path(parfait), id: "button-edit-#{parfait.id}" do %>
                              <i class='bi bi-pencil-fill'></i>
                            <% end %>
                            <%= link_to parfait_path(parfait), id: "button-delete-#{parfait.id}", data: { turbo_method: :delete, turbo_confirm: t('defaults.delete_confirm') } do %>
                              <i class="bi bi-trash-fill"></i>
                            <% end %>
                          </div>
                        <% end %>
                      </div>
                      <div>
                        <%= link_to parfait.shop.name, shop_path(parfait.shop) %>
                      </div>
                      <div class="card-text clamp-3"><%= parfait.body %></div>
                    </div>
                  </div>
                 </div>
              </div>
            <% end %>
          <% else %>
            <div class="mb-3">最新情報がありません</div>
          <% end %>
        </div>
      </div>
      <div class="text-end mt-2">
        <%= link_to '>>パフェ一覧を見る', parfaits_path, class: "btn btn-outline-primary" %>
      </div>
    </div>
  </div>
</div>
