<div class="container pt-5">
  <div class="row mb-3">
    <div class="col-lg-8 offset-lg-2">
      <h1>店舗詳細</h1>
      <article class="card">
        <div class="card-body">
          <div class="row">
            <div class="col-md-3">
              <%= image_tag 'noimage.png', width: "300", height: "200", class: "card-img-top img-fluid" %>
            </div>
            <div class="col-md-9">
              <h3 style="display: inline;"><%= @shop.name %></h3>
              <ul class="list-inline">
                <li class="list-inline-item"><%= @shop.full_address %></li>
              </ul>
              <div class='d-flex justify-content-end'>
                <%= link_to edit_shop_path(@shop), id: "button-edit-#@shop.id}" do %>
                  <i class='bi bi-pencil-fill'></i>
                <% end %>
                <%= link_to shop_path(@shop), id: "button-delete-#{@shop.id}", class: "ms-2", data: { turbo_method: :delete, turbo_confirm: t('defaults.delete_confirm') } do %>
                  <i class="bi bi-trash-fill"></i>
                <% end %>
              </div>
              </div>
            </div>
            <p>詳細はMVPの後で見れるようにします</p>
          </div>
        </article>
      </div>
    </div>
  </div>
</div>