<div class="my-3" data-controller="previews" data-previews-width="400">
  <%= f.label :パフェの画像 %><br>

  <%= f.file_field :parfait_image,
    direct_upload: true,
    data: { previews_target: "input", action: "change->previews#previewImage" } %>

  <div data-previews-target="preview" class="mt-2">
    <% if f.object.parfait_image.present? %>
      <%= image_tag f.object.parfait_image %>
    <% end %>
  </div>

  <% if f.object.parfait_image.present? %>
    <div>
      <%= f.check_box :remove_parfait_image %>
      <%= f.label :remove_parfait_image, "画像を削除する" %>
    </div>  
  <% end %>
</div>